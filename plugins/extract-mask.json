{
  "id": "extract-mask",
  "name": "Extract Mask",
  "description": "Extracts masks from images (alpha channel or creates mask from transparency/color).",
  "version": "1.0.0",
  "inputs": [
    {
      "id": "inputDir",
      "name": "Input Directory",
      "type": "directory",
      "description": "Directory containing images to extract masks from (PNG, JPG, TIFF, BMP, GIF, WEBP).",
      "required": true
    }
  ],
  "outputs": [
    {
      "id": "outputDir",
      "name": "Output Directory",
      "type": "directory",
      "description": "Directory where extracted masks will be saved."
    }
  ],
  "command": {
    "program": "docker",
    "args": [
      "run",
      "--rm",
      "lite/extract-mask:latest",
      "/input/inputDir",
      "/output"
    ]
  }
}

